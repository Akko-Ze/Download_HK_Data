name: 📨 Test Email Sending

on:
  workflow_dispatch:

jobs:
  test-email:
    runs-on: ubuntu-latest
    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v4

      - name: 🧾 Force error for testing
        id: check_log
        run: |
          # 强制触发错误，直接写 has_error=true
          echo "has_error=true" >> $GITHUB_OUTPUT
          echo "✅ TEST: 强制触发错误邮件发送" >> run_log.txt

      - name: 📧 Send test email to NetEase
        if: steps.check_log.outputs.has_error == 'true'
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.163.com
          server_port: 465
          username: ${{ secrets.EMAIL_USER }}
          password: ${{ secrets.EMAIL_PASS }}
          subject: "[TEST] 邮件发送功能测试"
          body: "这是一次测试邮件，用于验证 GitHub Actions 邮件发送是否正常。"
          to: your_email@163.com
          from: ${{ secrets.EMAIL_USER }}
          attachments: run_log.txt

      - name: 📤 Upload log to GitHub (optional)
        uses: actions/upload-artifact@v4
        with:
          name: test-log
          path: run_log.txt
